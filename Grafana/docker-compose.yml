services:
  grafana:
    container_name: grafana
    image: grafana/grafana:13.0.2
    restart: unless-stopped
    ports:
      - "3000:3000"

    networks:
      - network_swarm_public

    volumes:
      - /opt/docker/grafana/data:/var/lib/grafana

    environment:
      GF_SECURITY_ADMIN_PASSWORD: ${GF_SECURITY_ADMIN_PASSWORD}
      GF_DATABASE_MAX_OPEN_CONN: 300
      GF_ENABLE_GZIP: "true"
      GF_SECURITY_ALLOW_EMBEDDING: "true"
      GF_USERS_ALLOW_SIGN_UP: "false"
      GF_INSTALL_PLUGINS: alexanderzobnin-zabbix-app,grafana-clock-panel,marcusolsson-dynamictext-panel,gapit-htmlgraphics-panel,volkovlabs-variable-panel,victoriametrics-logs-datasource

networks:
  network_swarm_public:
    external: true
